审查 expo-app/ 目录本次所有改动，按以下五条逐一检查并报告：

1. 改动范围是否超出指令 — 有没有多做或少做。指令要求创建 BasicInfoScreen, GoalSelectScreen, PhotoAssessScreen, HomeScreen, TrainingScreen, DietScreen, ProgressScreen + 更新 App.tsx 和 types.ts
2. 是否改了不该改的文件 — 列出所有被修改的文件，对比 LoginScreen.tsx 原有内容是否被意外修改
3. 接口跟数据结构是否保持兼容 — navigation/types.ts 路由类型是否完整，import 路径是否正确
4. 异常状态有没有处理 — 每个页面的 loading/error/empty 状态
5. 有没有新增难维护的重复逻辑 — 相同样式或逻辑是否抽离

每条给出：通过 / 问题+修复建议。最后给总评：可以合并 / 需要修改。

还要检查 tsc --noEmit 能否编译通过。
