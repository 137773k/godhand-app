修改两个文件，完成后只输出 "DONE"，不要输出任何代码或解释。

## 文件1: screens/TrainingScreen.tsx

在现有代码基础上，新增以下功能：

1. **加号按钮**：在 SectionHeader 下方、训练列表上方，添加一个 "+" 按钮（余烬橙主题色），点击弹出添加动作的弹窗。

2. **添加动作弹窗 (Modal)**：
   - 半透明遮罩 + 居中卡片
   - 三个输入框：动作名称、组数×次数（如 "4 组 x 8 次"）、动作说明
   - "添加" 和 "取消" 按钮
   - 添加后新动作插入列表最前面，带一个小的自定义标记（比如 "⚡自定义" 标签）
   - 自定义动作的 id 用 `custom_${Date.now()}` 生成

3. **保持现有功能不变**：勾选完成、展开描述、完成训练按钮都正常工作。

## 文件2: screens/DietScreen.tsx

在现有代码基础上，改造饮食追踪功能：

1. **宏量素实际摄入追踪**：
   - 新增 state: `eatenMacros = { protein: 0, carbs: 0, fat: 0 }` 
   - 替换掉硬编码的 65% 演示数据

2. **宏量素进度条区域**：
   - 在原有的"宏量素分配"卡片下方新增一个"今日已摄入"卡片
   - 显示三个进度条：蛋白质、脂肪、碳水
   - 每个进度条显示 `已吃 g / 目标 g (占比%)`
   - 进度条颜色：蛋白质用余烬橙 #e0613a，碳水用金色 #f0a040，脂肪用暖黄 #e0c060

3. **食物记录输入区**：
   - 在"今日摄入"卡片内，进度条下方加一个快速记录区
   - 三个数字输入框（蛋白质g / 碳水g / 脂肪g）+ 一个"记录"按钮
   - 点击记录后累加到 eatenMacros，输入框清空
   - 累计热量自动计算（蛋白质×4 + 碳水×4 + 脂肪×9）

4. **每日重置**：
   - 组件挂载时检查日期，如果和上次记录日期不同，重置 eatenMacros 为 0

5. **保持现有功能**：BMR分析卡片、食谱列表等全部保留不动。
