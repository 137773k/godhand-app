回退所有 v9 锻造炉改动，恢复到 v8 黑金主题。

## 改动清单（全部执行）
1. theme.ts：恢复为 v8 黑金配色：
```
export const Colors = {
  bg: '#0B0B0B',
  bgSecondary: '#121212',
  surface: '#171717',
  surfaceElevated: '#1E1E1E',
  border: '#2A2A2A',
  textPrimary: '#F5F5F5',
  textSecondary: '#A1A1A1',
  textMuted: '#6B6B6B',
  textDim: 'rgba(107,107,107,0.5)',
  gold: '#C8A96A',
  goldStrong: '#D6B56D',
  goldPressed: '#A9884F',
  goldBorder: 'rgba(200,169,106,0.12)',
  goldLight: 'rgba(200,169,106,0.08)',
  goldGlow: 'rgba(200,169,106,0.06)',
  success: '#3DDC84',
  danger: '#FF4D4D',
  info: '#5E6AD2',
  gradientTop: '#121212',
  gradientBottom: '#0B0B0B',
} as const;

export const goldGradient = ['#C8A96A', '#A9884F'] as const;
export const goldGradientSoft = ['#D6B56D', '#C8A96A'] as const;
// 删除 bgGradient, emberGradient, emberGradientSoft
```
Typography、Spacing、Radius 保持不动。

2. 全局替换：
   - `ember` → `gold` (Colors.ember→Colors.gold, emberStrong→goldStrong, emberPressed→goldPressed, emberBorder→goldBorder, emberLight→goldLight, emberGlow→goldGlow)
   - `emberGradient` → `goldGradient`
   - `emberGradientSoft` → `goldGradientSoft`
   - `bgGradient` 导入 → 删除，ScreenContainer 恢复用 `[Colors.gradientTop, Colors.gradientBottom]`

3. 运行 npx tsc --noEmit 确保零错误

4. 运行 npx expo export -p web --max-workers 1 构建

项目路径: C:\Users\86180\Desktop\上帝之手\expo-app
